{
  "description": "An sparse encoding ingest pipeline",
  "processors": [
    {
      "sparse_encoding": {
        "model_id": "%s",
        "batch_size": "%d",
        "field_map": {
          "passage_text": "passage_embedding"
        }
      }
    }
  ]
}
