{
  "description": "An example fixed token length chunker pipeline",
  "processors" : [
    {
      "chunking": {
        "field_map": {
          "body": "body_chunk"
        },
        "algorithm": {
          "fix_length": {
            "token_limit": 10
          }
        }
      }
    }
  ]
}
