{
  "request_processors": [
    {
      "neural_sparse_two_phase_processor": {
        "tag": "neural-sparse",
        "description": "This processor is making two-phase rescorer.",
        "enabled": true,
        "two_phase_parameter": {
          "prune_ratio": %f,
          "expansion_rate": %f,
          "max_window_size": %d
        }
      }
    },
    {
      "neural_query_enricher": {
        "tag": "tag1",
        "description": "This processor is going to set the default model id.",
        "default_model_id": "%s"
      }
    }
  ]
}
